import { McpServer } from "@modelcontextprotocol/sdk/server/mcp";

export const server = new McpServer({
    name: "domo_mcp",
    description: "A MCP server for Domo",
    version: "1.0.0",
    title: "domo_mcp",
});
